import 'package:flutter/material.dart';

class CharacteDetailsScreen extends StatelessWidget {
  const CharacteDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Nadaaa'),
    );
  }
}
